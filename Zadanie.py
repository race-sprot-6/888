first_list = [3,4,5,1,1]
#first_list = {1: "a", -5: "v", 4: "x"}

#second_list = first_list.sort(reverse=True) #только для списков
#print("first_list =", first_list, "second_list =", second_list)

#second_dict = sorted(first_list)
#print("second_dict =", second_dict, "type", type(second_dict))

#second_dict = sorted(first_dict.values)

first_set = set(first_list)
print('first_set =', first_set)

#set_from_str = set('pppyton')
#print('set_from_str =', set_from_str)

#sum(name, [])
#from itertools import chain

#list(chain.from_iterable(name))
#s = ' '.join([' '.join(i) for i in name])

#map(''.join for x in name)
#first_set = set(name)
#nums = list(map(int, name))